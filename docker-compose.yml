version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: scleorg-db
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: scleorg
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
